import numpy as np
import MinuitFit as MiF
import matplotlib.pyplot as plt

def func(x, *par):
	a=par[0]
	b=par[1]
	c=par[2]
	return y

if __name__=='__main__':
	data_x=np.array([])
	data_y=np.array([])
	x_err=np.array([])
	y_err=np.array([])
	ax.plt.subplot()
	ax.
	MiF.Setup(data_x,data_y,x_err,y_err,func,ax)
	MiF.SetRange()
	MiF.SetLimit()
	result=MiF.chisquare()
	plt.title()
    plt.xlabe()
    plt.ylabel()

